import React from 'react';
// import AddNote from './AddNote';
// */}

const Home = (props) => {
    return (
        <div className="">
            {/* <AddNote/> 
            <Note showAlert={props.showAlert} />*/}
        </div>
    );
}

export default Home
